# 个人钱包 PWA

一个基于 `Vue 3 + Vite + TypeScript` 的 iPhone 优先 PWA，用来保存图片卡片和文字卡片。

## 功能

- 图片卡片：支持相册选择和直接拍照
- 文字卡片：支持自定义名称、正文和备注
- 首页列表：浏览全部卡片
- 卡片详情：左右滑动切换、编辑、删除
- 本地离线保存：数据保存在当前设备

## 运行

```bash
npm install
npm run dev
```
